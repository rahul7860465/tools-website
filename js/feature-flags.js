export const featureFlags = Object.freeze({
  // Tool enable/disable flags by tool id.
  tools: Object.freeze({
    // Example:
    // "jwt-decoder": true,
  }),

  // Mark premium tools (UI only, no paywall).
  premiumTools: Object.freeze({
    // Example:
    // "regex-tester": true,
  }),
});

